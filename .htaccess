RewriteEngine On

# Laisser passer les ressources existantes
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Réécriture vers le front controller
RewriteRule ^ index.php [QSA,L]
